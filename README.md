# My Pay

Monorepo layout:

- `frontend/` — React + TypeScript + Vite
- `backend/` — API (placeholder)

## Frontend

From the repository root:

```bash
npm run dev
```

Or from `frontend/`:

```bash
cd frontend
npm run dev
```

Other scripts: `build`, `lint`, `preview` (same from root or inside `frontend/`).
